'use strict';

const defaultHashTableSize = 20;
const customHashTableSize = 10;

const key = 'test';
const value = 10;
const indexInArray = 18;

module.exports = {
  defaultHashTableSize,
  customHashTableSize,
  key,
  value,
  indexInArray
};